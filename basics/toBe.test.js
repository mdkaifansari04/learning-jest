test("test strick checking", () => {
  expect(1 + 1).toBe(2);
  expect("hello").toBe("hello");

  // let obj = { name: "kaif" };
  // expect(obj).toBe({ name: "kaif" }); // it fails because it has checks primitive values not
});
